#include "utilities.h"

void Delay_ms(unsigned int delay_ms )
{
    int i , j ;
    for(i = 1 ;i<=1000;i++)
    {
        for(j = 1 ;j<= delay_ms ; j++)
        {
            /* do nothing */
        }
    }
}
